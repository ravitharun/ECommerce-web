[build]
  base = "Client"                           # 👈 run build inside /Client
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"                          # 👈 since base=Client, dist is relative

[build.environment]
  NODE_VERSION = "20.17.0"
